<section class="box">
  <header>
    <h2>Latest Post</h2>
  </header>
  <section class="post">
    <h3><%= @post.title %></h3>
    <p class="small">By <%= @post.user.username %> on <%= format_date(@post.created_at) %></p>
    <%= @post.body.truncate(255) %>
  </section>
</section>
